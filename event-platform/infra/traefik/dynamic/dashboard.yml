http:
  routers:
    traefik-dashboard-http:
      rule: "PathPrefix(`/dashboard`) || PathPrefix(`/api/rawdata`) || PathPrefix(`/api/overview`)"
      entryPoints:
        - web
      service: api@internal
      middlewares:
        - grafana-auth@file
