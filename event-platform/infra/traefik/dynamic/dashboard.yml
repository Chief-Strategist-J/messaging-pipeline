http:
  routers:
    dashboard:
      rule: "Host(`traefik.${DOMAIN_SUFFIX}`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"
      entryPoints:
        - metrics
      service: api@internal
      middlewares:
        - grafana-auth@file
      tls: {}

  services: {}
