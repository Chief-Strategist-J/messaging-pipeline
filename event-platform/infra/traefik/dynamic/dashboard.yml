http:
  routers:
    dashboard:
      rule: "Host(`traefik.scaibu.localhost`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"
      entryPoints:
        - metrics
      service: api@internal
      middlewares:
        - grafana-auth@file
      tls: {}
